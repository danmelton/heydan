class <%=@class_name%> < HeyDan::Script

  def version
    1
    #increment this integer by if you change this script
  end

  def type
    'dataset'
    #can be 'dataset', 'attribute' or 'identifier'
    #use 'attribute' if you want to add string data like an address, name, website, etc
    #use 'identifier' if you want to add a new identifier to the identifiers block
  end

  def build
    #this block builds a csv file from the @data variable
    @data = HeyDan::Helper::get_data_from_url('your_url')
    #you can grab a file on the web (zip, csv, txt) which will load into the @data variable
    #next, you will need to format the @data variable. This 
    @data = @data.map { |c| [c[1], c[0]]}
    
    #lastly, you should add a first row with the main identifer_id as the first row/column.
    @data.unshift(['ansi_id', 2015])
    #if this is an identifier, @data.unshift(['ansi_id', 'cds_code_id'])

    debug
    #launches an interactive debugger
    
    #That's it. HeyDan will fetch the url file into downloads, format it, save a copy to the datasets and then use that to update your files.
  end

end